files:
  "/etc/cron.d/mycron":
    mode: "000644"
    owner: root
    group: root
    content: |
      * * * * * root python manage.py scheduler >> /var/log/scheduler.log 2>&1
commands:
  remove_old_cron:
  command: "rm -f /etc/cron.d/mycron.bak"